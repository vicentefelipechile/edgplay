/**
 * migrate:apply
 *
 * Reads pending migration files from ./migrations/ and applies them
 * to the configured D1 database via wrangler d1 execute.
 *
 * TODO: implement
 */
export async function apply(_args: string[]): Promise<void> {
  console.log("migrate:apply — TODO");
}
