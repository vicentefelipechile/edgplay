/**
 * migrate:rollback
 *
 * Reverts the last applied migration by executing its down SQL.
 *
 * TODO: implement
 */
export async function rollback(_args: string[]): Promise<void> {
  console.log("migrate:rollback — TODO");
}
