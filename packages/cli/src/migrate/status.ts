/**
 * migrate:status
 *
 * Lists all migration files and marks each as applied or pending,
 * by querying the _edgplay_migrations table in D1.
 *
 * TODO: implement
 */
export async function status(_args: string[]): Promise<void> {
  console.log("migrate:status — TODO");
}
